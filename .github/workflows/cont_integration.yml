on: push

name: CI

jobs:
  
  csharp:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: extractions/setup-just@v1
      - run: just build-bindings-lib
      - run: ls target/release
