# AMP2 mock

This crate has the sole purpose of making AMP2 testing in LWK simpler.

`amp2_mock` executable can be run locally or in CI to test AMP2 features.
It must never be run in production.

Implementation should be minimal.
New dependencies should be avoided.

This crate should have no tests.
