# LWK app

LWK JSONRPC Server Application.

This is intended to be used by `lwk_cli`.

All requests and responses data model are in `lwk_rpc_model`.

## Tests
```
cargo test
```

Most test coverage is in `lwk_cli` tests.
