Swap data used by the ignored `test_boltz_store_fs` test in `lwk_boltz/src/store.rs`.

Mnemonic: "craft travel attitude order useful orient venue true double motor enable already"

Contains a single pending swap: an unpaid invoice from a reverse swap.
