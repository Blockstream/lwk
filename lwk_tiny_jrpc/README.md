# LWK Tiny JSONRPC Server

Tiny JSONRPC server.

This crate is intended to be used only by `lwk_app`, which is then used by `lwk_cli`.
It should be minimal and only expose features used by `lwk_app`.

## Tests
```sh
cargo test
```
